# Implementation Plans

Generated by the improve skill (deep audit) on 2026-08-15 against commit `7453aee`.
Second audit round (adversarial technical audit) planned 2026-08-15 against
commit `b0dd06a` — plans 015-020. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## ARCHIVE RULE (hard rule)

**All tasks/plans/requests that are DONE are automatically moved to
`plans/archive/`** — the moment a plan's status becomes DONE (after review and
merge), move its file to `plans/archive/` in the same change that marks it done.
The index below keeps the historical record; the archived files are the record
of what was executed. Do not delete archived plans.

## Status (27 plans; 001-027 all DONE — files archived)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Guard cancelled-sale delete/edit against double inventory restore | P1 | S | — | DONE (executed 2026-08-15, commit 86063b1, branch advisor/001-cancelled-sale-guard, reviewed APPROVED) |
| 002  | Unify model validation: service-layer enforcement, no load-time crashes | P1 | M | — | DONE (executed 2026-08-15, commit e701f73, merged, reviewed APPROVED) |
| 003  | PIN security: rotate, relocate config, PBKDF2, persistent lockout | P1 | M | — | DONE (archived file 003-pin-security.md exists; prior index row was left stale — code verified: PBKDF2-SHA256 600k, config 0600, legacy-hash rejection in ui/login_dialog.py:36-47,205-212) |
| 004  | Reconcile index sources + cleanup migration + legacy-DB upgrade test | P1 | M | — | DONE (executed 2026-08-15, 5 commits 13c7b92..144b6e3, merged, reviewed APPROVED) |
| 005  | Sales-table batching + refresh-wave coalescing | P2 | M | — | DONE (executed 2026-08-15, commits 941c8ef/8d445f1, merged, reviewed APPROVED) |
| 006  | Fix xdist crash + enable parallel CI + bump CI actions | P1 | S | — | DONE (executed 2026-08-15, commits dcaf970/ad347a3/d7fd8cf, branch advisor/006-xdist-ci, reviewed APPROVED) |
| 007  | Unify cache protocol; fix false clear_cache pairing | P2 | M | — | DONE (executed 2026-08-15, commits 0981f5a/e0116a2, merged f4937c3, reviewed APPROVED) |
| 008  | Sale-domain test hardening + purchase-delete reversal + suite cleanup | P1 | S-M | 007 | DONE (executed 2026-08-15, commits c1ebf4b/41c0041, merged 02a97e5, reviewed APPROVED) |
| 009  | Inventory + analytics real-DB guard tests | P2 | M | — | DONE (executed 2026-08-15, commits 943c0b4/9ea88fa/d4dc040, merged 7e3ed71, reviewed APPROVED; included root-cause fix for event_system reload fixture flake) |
| 010  | Receipt + purchase_query characterization tests | P2 | M | — | DONE (executed 2026-08-15, commits 733f128/4d30c83/81d2fd7, merged, reviewed APPROVED; incl. production fix: validate_filepath for receipt PDFs) |
| 011  | Dead code sweep with zero-reference guard | P2 | M | 008, 009, 010 | DONE (executed 2026-08-15, commits d179e7d/1acdba2/6c6e3f4, merged, reviewed APPROVED; 2045 deletions; follow-up: get_sale_statistics + handle_external_service + ReceiptService.send_via_whatsapp orphans) |
| 012  | DX: runnable README, pre-commit install + drift hook, logging paths, editorconfig | P2 | S-M | — | DONE (executed 2026-08-15, 4 commits + f065a62 logger-name fix, merged, reviewed APPROVED; incl. production fix: app logger name aligned with yaml so logging is not dropped) |
| 013  | Reconcile docs/backlogs with reality; language decision; bandit wire-or-drop | P2 | S-M | — | DONE (executed 2026-08-15, 4 commits 10adf92..b3b362a, merged, reviewed APPROVED; bandit wired with 8 false-positive nosec sites) |
| 014  | Extend pyright scope into ui/ incrementally | P3 | M | — | DONE (executed 2026-08-15, 8 commits e62d4b1..dc61586, merged, reviewed APPROVED) |
| 015  | Exclude cancelled sales from every revenue/profit/statistics/turnover/movement report | P1 | S | — | DONE (executed 2026-08-15, commits 5b924e2/02234d1, merged 094d15f, reviewed APPROVED; full suite 337 passed) |
| 016  | Stop logging customer PII; make log files owner-only | P1 | S | — | DONE (executed 2026-08-15, commits e02bfb1/b15b614/6f38c4c, merged, reviewed APPROVED; rotation-time hardening deferred to next setup by design) |
| 017  | `validate_money` must reject fractional values in string form | P1 | S | — | DONE (executed 2026-08-15, commits d9bacc5/43bdf46, merged, reviewed APPROVED) |
| 018  | Route manual UI inventory edits through the adjustment ledger | P2 | S | — | DONE (executed 2026-08-15, commits c2b15f9..fdf5eec, merged 950085b, reviewed APPROVED; reason literal amended to validator-safe "manual set"; backlog: CURRENT_TIMESTAMP vs date-only movement bound) |
| 019  | Make analytics date-range queries index-usable | P2 | M | 015 | DONE (executed 2026-08-15, commits 8e5ccac/9901453, merged 8b34c81, reviewed APPROVED; boundary test fixed next-day-midnight semantics) |
| 020  | Sad-path test coverage for the update-sale workflow and MutationCoordinator | P2 | S | — | DONE (executed 2026-08-15, commit 25b506c, merged c815fd4, reviewed APPROVED) |
| 021  | InventoryAgingMetric must not count cancelled sales as "last sold" | P2 | S | — | DONE (executed 2026-08-15, commits 7d446d3\/36ee211\/864d292, merged, reviewed APPROVED) |
| 022  | Multi-business support (CasaBea): one DB per business + startup selector | P1 | M | — | DONE (executed 2026-08-15, 8 commits b3aa38b..fa38ee7, merged 5bbed86, reviewed APPROVED; Phase E amendment: analytics engine follows active business; deviation: three-branch db_path resolution preserves DATABASE_NAME override + legacy test seam) |
| 023  | Copy customers (identity only) between business DBs (El Rincón → CasaBea) | P1 | S | — | DONE (executed 2026-08-16, commits db31700/a3f73ce, merged 4bfaa74, reviewed APPROVED; executor STOPPED on live-DB-vs-repo schema drift → plan revised to repo schema; drift recorded as backlog) |
| 024  | Reconcile customer schema drift (current_balance/credit_limit in repo sources) | P2 | S | — | DONE (executed 2026-08-16, commits 1939f9e..d1f16dd, merged 955e4c2, reviewed APPROVED; no-op verified against a copy of the live DB; remaining COLLATE NOCASE/name-CHECK drift moved to backlog) |
| 025  | In-app business switch + config registry self-healing | P1 | S | — | DONE (executed 2026-08-16, 8 commits, merged 00acf5d, reviewed APPROVED; executor STOPPED on engine.py legacy-seam interaction → amendment: registry-explicitness flag; second STOP caught order-dependent flag staleness → one-line Config.set fix) |
| 026  | `isolate_config` teardown must never save (config-nuking test bug) | P1 | S | — | DONE (executed 2026-08-16, commits 5ce9b7d..870bda2, merged, reviewed APPROVED; real config nuked by suite at 12:55 → restored from backup; post-merge full suite leaves real config untouched — verified mtime+content) |
| 027  | Per-business dashboard KPI profiles (reseller vs production) | P2 | S-M | — | DONE (executed 2026-08-16, 5 commits, merged 4027e03, reviewed APPROVED; post-merge suite 395 passed; one un-reproduced 75-fail/29-error first run in the worktree — never recurred in 5 runs, monitor if it resurfaces) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 008 depends on 007: 007 defines the post-cleanup cache contract that 008's
  cache-freshness tests assert. If 007 is executed first, its changes are small
  and behavior-preserving; do not run 008's cache assertions against the
  pre-007 behavior.
- 011 depends on 008/009/010: the dead-code sweep must not delete methods that
  the new tests pin. Run the zero-reference guard AFTER the test plans land.
- 004's migration touches schema.sql/indexes; run `scripts/check_schema_drift.py`
  after it (the plan's own verification covers this).
- 013 resolves the SPECIFICATIONS.md language contradiction before any future UI
  string work (no dependency on other plans, but do it before writing UI strings).
- 019 depends on 015: both edit the WHERE clauses of `services/analytics/metrics.py`.
  Run 015 first (adds the `status` filters + tests), then 019 rewrites the date
  predicates; 019 must preserve whatever 015 added.
- 020 is a pure test plan: run it before any future refactor of
  `update_sale_workflow.py` (it is the characterization layer for that file).

## Findings considered and rejected (so nobody re-audits them)

- **Sale/purchase ledger dedup (ARCH-04)**: high-churn, high-risk refactor of
  High-Risk Areas for structural gain; the *correctness* half (quantity stored
  as float vs TEXT) is folded into plan 004's data-normalization step.
- **Unify post-commit finalization across customer/product/category (ARCH-05)**:
  behavior-adjacent changes to UI refresh signals for structural consistency;
  not worth the regression risk today. Keep using MutationCoordinator for new
  mutation workflows per AGENTS.md.
- **Analytics second connection (ARCH-08)**: read-only by contract and enforced
  in practice; documenting the exemption is folded into plan 013. No code change.
- **utils/helpers.py junk drawer split (ARCH-09)**: cosmetic; low value.
- **Decorator stacking consolidation (ARCH-10)**: behavior-preserving but touches
  High-Risk service paths for cosmetic gain; not worth the churn.
- **sale_view god module + dialog dedup (ARCH-06/07)**: real but large (L);
  deferred — revisit after 014 (type coverage) lands, which will make the split safer.
- **Purchase update through workflow (ARCH-11)**: MED/MED; the pre-validation
  gap is worth a follow-up issue, not a plan this round.
- **Dashboard/purchase-view UI tests (TESTS-03)**: Qt interaction tests are the
  flakiest layer; narrow smoke tests deferred until 006 (xdist) lands.
- **Refresh-storm full debounce redesign**: plan 005 takes the batching half and
  a conservative coalescing step; a full worker-thread offload is out of scope.
- **reportlab 5.0 upgrade**: blocked on a product decision; cap stays.
- **`-n auto` in CI**: only after 006.
- **Direction items (returns/refunds, WhatsApp, ux golden paths, CSV import,
  headless CLI, PIN recovery)**: product decisions — not planned; see
  "Direction candidates" below.

## Second audit round (2026-08-15, commit b0dd06a): audited but NOT planned

Vetted findings from the adversarial audit that were not selected for plans
this round. Do not re-report them in the next audit; turn them into plans only
if the maintainer asks.

- **Duplicate identifiers/names surface as raw UNIQUE-constraint errors**
  (customer_service create/update, category create/update) — S/LOW; error-message quality.
- **Purchase flow never verifies product exists** (purchase_service.py:219-223) — S/LOW; mirrors sale validation.
- **`rollback_transaction` breaks nested-transaction lock accounting**
  (database_manager.py:241-251) — latent, no nested callers today; S/LOW.
- **`receipt_id` stale after sale-date edit** (update_sale_workflow.py never
  regenerates it) — S/LOW-MED; run plan 020's characterization tests first.
- **`purchases` missing date/supplier indexes** (purchase_query_service.py:76,122,162,193,234) — S/LOW; needs an Alembic revision + drift-check registration.
- **`theme` config key is a no-op** while spec promises 3 themes
  (config.py:213; ui/styles.py hardcodes dark) — S-M/LOW; implement or remove.
- **Coverage tooling installed but never runs** (CI + addopts invoke bare
  pytest) — S/LOW; instrument first, gate later.
- **`get_all_purchases` — zero tests, no pagination, lru_cache(maxsize=1)**
  (purchase_query_service.py:39-43) — S-M/MED.
- **Backup files created 0664, chmod 0600 only after copy; legacy backups stay
  0644** (backup_service.py:84-95) — S/LOW.
- **Config fallback path serves PIN state from a never-chmod'd repo copy**
  (config.py:108-122,140,296-301) — S/LOW.
- **Duplicate `_get_product_ids` ×3 + near-duplicate sale-item hydration ×3**
  (sale_service, purchase_service, mutation_coordinator) — S-M/LOW; DRY.
- **~15 dead public service/validator methods + ~20 dead model mutation
  methods** (product_service.py:309, customer_service.py:114,
  inventory_service.py:258, validators.py:143-357, models/*) — S-M/LOW-MED;
  reuses plan 011's zero-reference guard.
- **Sale item price/quantity unbounded vs purchase caps**
  (sale_service.py:483-490) — S/LOW.
- **Category service wraps NotFoundException → DatabaseException**
  (category_service.py:73-86,95-106) — S/LOW.
- **Stale docs**: perf.md lists dropped indexes, security_findings.md cites
  deleted excel_exporter.py, readme says weekly backups vs daily default — S/LOW.
- **Per-item `get_product` in sale-item validation loop** (up to 1000 per
  sale) — S/MED; batch via IN query, low frequency.
- **`test_perf_backup` thread-leak on assertion failure** (no try/finally) — S/LOW.
- **Both Alembic revisions have no-op `downgrade()`** — S/LOW; document as unsupported.

## Follow-up backlog (found during execution of plans 015-020)

- **InventoryAgingMetric counts cancelled sales as "last sold"**
  (`services/analytics/metrics.py:193` — `MAX(s.date)` with no status filter;
  plan 015 scoped only the 9 date-range metrics). One-line filter + test; S/LOW.
- **`get_inventory_movements` can't see same-day late adjustments**
  (adjustment rows stamped `CURRENT_TIMESTAMP` in UTC sort after a date-only
  end bound, and tomorrow is rejected as future by `validate_date`; surfaced by
  plan 018's test). Movement-query date semantics; S/MED.
- **Per-plan drift checks show plan-015 diffs** for 018/020 — resolved via
  dispatch-time drift notes; future plans should scope drift checks tighter.

## Follow-up backlog (found during execution of plans 015-023)

- **RESOLVED by plan 024**: `customers.current_balance`/`credit_limit` live-vs-repo drift — columns now in `models/customer.py`, `schema.sql`, migration `652b05c0c11e`; no-op verified on a copy of the live DB.
- **Remaining drift (open)**: live `customers.identifier_9 COLLATE NOCASE` and the name-length CHECK differ from repo sources — future reconciliation plan if it ever bites.
- **`customer_identifiers.identifier_3or4` is NOT NULL** in all repo sources — copy scripts and future tooling must skip the row, never insert NULL.

## Direction candidates (not written as plans — maintainer's call)

1. Returns/refunds (devolución) workflow — primitives all exist; design/spike first.
2. WhatsApp receipt send — currently a logging placeholder (`receipt_service.py:87-98`).
3. ux_revamp golden paths remaining items (barcode `*N` multiply, `+24` add-stock, low-stock jump).
4. Product CSV import (menu stub + orphaned `data_import_completed` signal exist).
5. Headless CLI (restore/import/reconcile) — thin over the Qt-free service layer.
